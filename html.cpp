<html>
/* TODO: FIX HTML */

#define using namespace OZARON;
using using;
ozaron; using;

int(main(){
  #include(ozaror)->ESP(  );
  ESP( OZAROR./*ozaron#5101 discord subscribe*/OZAROR->ESP( FAKEDUK )  );
  bSendPacket( *OZAROR->FAKE_ESP( WALHAK ) );

  egiste( "BAYONET" ).r<Bayonet>print( "Bayonet Hacked\a\\a\a\a\a\a\a\a\a\\a\a\a\n" );

    FindSegnature( GetProccessHandle( "BAYONET" )) )->Hacked()->Signature("Ozaron#5101")->AIMBOT()
});

DWORD INJECT->BSendPacket(ESP);
